spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update   # 개발 단계에서는 update, 운영은 validate/recreate 권장
    show-sql: true
    properties:
      hibernate:
        format_sql: true
